spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost:3306}/cryptowatcher
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASS:root}
  jpa:
    hibernate:
      ddl-auto: update


multiplecoinsURL: https://api.coinlore.net/api/ticker/?id=80,90,48543
logging:
  file:
    name: logging.log